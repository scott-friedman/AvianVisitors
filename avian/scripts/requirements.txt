# Image pipeline dependencies for avian/scripts/.
# pregen.py needs only Pillow (for reference downscaling); it degrades
# gracefully without it. build_masks.py needs Pillow. verify.py is
# standard-library only.
Pillow>=10.0

# keycut.py — the DEFAULT step-2 cutter on this Mac (cutout.py's onnxruntime
# hangs here; see the README). numpy is a hard import; scipy is optional
# (better feathering) but cheap.
numpy>=1.24
scipy>=1.10

# cutout.py only — rembg + onnxruntime for the BiRefNet matting model. Majors
# capped: a future major could break the new_session/remove API. Skip these if
# you're using keycut.py.
rembg>=2.0.76,<3
onnxruntime>=1.16,<2
